[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "unsafe2safe"
version = "0.1.0"
description = "Project-specific components for privacy-preserving image editing"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
